try executing the commands with system(). Implement pipes.

call function executing commands with parameters:
	- a pointer to the first command to be executed (linked list)
	- the input pipe (int tab[2], NULL for fist command)

fork. (parent returns)
close the useless end of the pipe.
redirect stdin
if not last command:
	create out pipe
	close useless end
	dup stdout
	create child with parameters: ptr to next command and out pipe.
call system()