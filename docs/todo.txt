2. Do tokenization (for each commadn in the pipeline)
	- The redirection operators and their associated token (filename)
	  are stored in a separated linked list.
	- All other tokens are in another linked list.
3. Implement variable expansion.
4. Pass both lists (redirections and command) to the subshell.